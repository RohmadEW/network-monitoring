import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.network.monitoring',
  appName: 'Network Monitoring',
  webDir: 'out'
};

export default config;
